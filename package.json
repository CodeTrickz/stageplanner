{
  "name": "stageplanner",
  "private": true,
  "version": "1.0.2",
  "scripts": {
    "prepare": "husky install",
    "lint:commits": "commitlint --from=HEAD~1",
    "release": "standard-version --tag-prefix v --preset conventionalcommits && node scripts/create-github-release.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "husky": "^9.0.11",
    "standard-version": "^9.5.0"
  }
}
